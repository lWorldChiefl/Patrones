@model Proyecto_Final.Models.ImagenCompleta

@{
    ViewBag.Title = "Crear";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Crear</h2>

<form action="" method="post" enctype="multipart/form-data">
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Producto</h4>
        <hr />
        @Html.ValidationSummary(true)
        <div class="form-group">
            @Html.Label("Nombre", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.TextBoxFor(model => model.Producto.proyectName, new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.Producto.proyectName)
            </div>
        </div>

        <div class="form-group">
            @Html.Label("Precio", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.TextBoxFor(model => model.Producto.proyectDescription, new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.Producto.proyectDescription)
            </div>
        </div>

        <div class="form-group">
            @Html.Label("Stock", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.TextBoxFor(model => model.Producto.proyectStock, new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.Producto.proyectStock)
            </div>
        </div>

        <div class="form-group">
            @Html.Label("Categoria", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownListFor(x => x.Producto.categoryId, (IEnumerable<SelectListItem>)ViewBag.tipo_Categoria, new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.Producto.categoryId)
            </div>
        </div>

        @Html.HiddenFor(model => model.Producto.proyectImage)

        <div class="form-group">
            @Html.Label("Imagen", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.TextBoxFor(model => model.Imagen.ImageUpload, new { type = "file" })
                @Html.ValidationMessageFor(model => model.Imagen.ImageUpload)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Crear" class="btn btn-success" />
            </div>
        </div>
    </div>
</form>
